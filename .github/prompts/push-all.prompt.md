---
agent: agent
---
* run the shell command "git push all"
* When the output is "Everything up-to-date" you can close the terminal window and run the command "exit"
* When there are other messages like error messages, then leave the terminal window open
